{
  "name": "on-demand-resolver",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist || true && tsc --build",
    "test": "npm run build && node --test dist/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "zod": "^4.6.5"
  }
}
